//a program for multiply 2 numbers.
#include <stdio.h>

int main() {
    float a, b;

    printf("Enter two numbers: ");
    scanf("%f %f", &a, &b);

    printf("Multiplication result: %.2f\n", a * b);

    return 0;
}
